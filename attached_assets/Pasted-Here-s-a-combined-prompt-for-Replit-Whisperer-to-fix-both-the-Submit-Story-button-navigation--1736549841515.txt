Here’s a combined **prompt for Replit Whisperer** to fix both the **Submit Story button navigation** issue and verify the **Share to Social Media features**:

---

### **Prompt for Replit Whisperer: Fix and Verify Button Navigation and Social Sharing**
> **Objective**: 
> 1. Fix the issue where the `Submit Story` button does not navigate to the submission page.
> 2. Verify and fix the `Share to Social Media` feature to ensure it works as expected. Follow these steps:
>
> ---
>
> ### **Part 1: Fix Submit Story Button Navigation**
> #### **1. Verify the Button’s HTML Structure**
> - Ensure the `Submit Story` button is correctly defined in the HTML:
>   ```html
>   <button id="submit-story-btn" onclick="navigateToSubmitPage()">Submit Story</button>
>   ```
> - If using an `<a>` tag, ensure the `href` attribute points to the correct route:
>   ```html
>   <a href="/submit" id="submit-story-btn">Submit Story</a>
>   ```
>
> #### **2. Check the JavaScript Event (if applicable)**
> - Confirm the JavaScript function is correctly implemented for button navigation:
>   ```javascript
>   function navigateToSubmitPage() {
>       window.location.href = "/submit";
>   }
>   ```
> - Add a debugging log to verify the function fires when the button is clicked:
>   ```javascript
>   console.log("Submit Story button clicked");
>   ```
>
> #### **3. Verify the Backend Route**
> - Ensure the `/submit` route is implemented in the backend:
>   ```python
>   @app.route("/submit", methods=["GET", "POST"])
>   def submit_story():
>       return render_template("submit.html")
>   ```
> - Confirm the `submit.html` file exists in the `/templates` directory.
>
> #### **4. Test and Validate**
> - Test the button in the app:
>   - Ensure it navigates to `/submit`.
>   - Check the server logs for errors if the page doesn’t load.
> - Directly test the `/submit` route in the browser:
>   - Visit `http://localhost:5000/submit` (or your app’s deployed URL).

---

> ### **Part 2: Verify Share to Social Media Features**
> #### **1. Verify Share Button Structure**
> - Check that the share buttons are properly defined in the HTML:
>   ```html
>   <button id="share-facebook" onclick="shareToFacebook()">Share to Facebook</button>
>   <button id="share-twitter" onclick="shareToTwitter()">Share to Twitter</button>
>   ```
>
> #### **2. Check JavaScript for Social Sharing**
> - Verify the JavaScript functions for each platform:
>   - **Facebook**:
>     ```javascript
>     function shareToFacebook() {
>         const storyUrl = window.location.href;
>         const facebookUrl = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(storyUrl)}`;
>         window.open(facebookUrl, '_blank');
>     }
>     ```
>   - **Twitter**:
>     ```javascript
>     function shareToTwitter() {
>         const storyUrl = window.location.href;
>         const tweetText = "Check out this amazing story!";
>         const twitterUrl = `https://twitter.com/intent/tweet?text=${encodeURIComponent(tweetText)}&url=${encodeURIComponent(storyUrl)}`;
>         window.open(twitterUrl, '_blank');
>     }
>     ```
> - Add debugging logs to confirm the functions are being called:
>   ```javascript
>   console.log("Sharing to Facebook:", storyUrl);
>   console.log("Sharing to Twitter:", storyUrl);
>   ```
>
> #### **3. Test Share Button Behavior**
> - Click the share buttons and verify they:
>   - Open the correct social media sharing page.
>   - Prepopulate the URL and any additional text correctly.
>
> #### **4. Handle Missing or Invalid Data**
> - Ensure the `storyUrl` is dynamically generated and valid:
>   ```javascript
>   const storyUrl = `/story/${storyId}`;
>   if (!storyUrl) {
>       console.error("Story URL is missing or invalid.");
>   }
>   ```

---

> ### **Test and Deliverables**
> - **For Submit Story**:
>   - The button navigates the user to the `/submit` page.
>   - The `/submit` route loads correctly, and errors are handled gracefully.
> - **For Social Sharing**:
>   - All share buttons (e.g., Facebook, Twitter) work as expected.
>   - Debugging logs confirm that the correct URLs are used for sharing.
>   - Errors such as missing `storyId` or invalid URLs are handled gracefully.

---

This prompt ensures both issues are addressed in a clear and systematic way. Let me know if you need additional help! 🚀